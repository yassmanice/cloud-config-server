server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yassmanice/config-data.git
          search-paths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}